rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Пользователь может читать и писать только свой профиль и прогресс
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
      match /progress/{doc} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
    }
  }
}
